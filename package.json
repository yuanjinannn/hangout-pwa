{
  "name": "hangout-pwa",
  "version": "0.2.0",
  "private": false,
  "description": "A mobile-first PWA prototype for planning hangouts with places, voting, sharing, and public activity cards.",
  "scripts": {
    "build:pages": "node scripts/build-pages.js",
    "check:syntax": "node --check app.js && node --check app-data.js && node --check app-logic.js && node --check server.js && node --check sw.js && node --check scripts/smoke-ui.js",
    "smoke": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/smoke-ui.ps1"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  },
  "engines": {
    "node": ">=20"
  }
}
